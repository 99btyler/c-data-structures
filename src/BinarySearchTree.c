#include <stdio.h>

struct Node {
    int data;
    struct Node *leftChildNode;
    struct Node *rightChildNode;
};

int main() {

    printf("\nBinarySearchTree\n");

    struct Node *firstNode = NULL;

    return 0;

}