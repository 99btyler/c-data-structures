#include <stdio.h>

struct Node {
    int data;
    struct Node *nextNode;
    struct Node *previousNode;
};

int main() {

    printf("\nCircularDoublyLinkedList\n");

    struct Node *firstNode = NULL;

    return 0;

}